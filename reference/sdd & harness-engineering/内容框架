这份宣讲文案将为您 90 分钟分享中的核心环节（约 40 分钟）提供深度支持。内容聚焦于**规范驱动开发 (SDD)** 和 **Harness（工程/环境）工程**，旨在帮助初学者建立系统认知，同时为进阶者提供架构层面的启发。

---

### 宣讲主题：从“氛围编程”到架构自治——SDD 与 Harness 工程实战
**目标时长：40 分钟**

---

#### 第一部分：告别“氛围编程” (Vibe Coding) 的幻觉（5 分钟）

**开场：**
在 AI 编程的初期，我们常被那种“对话即所得”的魔力所吸引。只要描述一个想法，AI 就能吐出一串代码。这种依赖非结构化指令、凭感觉编写代码的模式，被称为**“氛围编程” (Vibe Coding)**。

**痛点解析：**
*   **黑盒谈判**：我们不是在进行工程设计，而是在与一个“黑盒”讨价还价。
*   **上下文漂移**：AI 可能会在修复一个 Bug 的同时，因为没看到代码库深处的逻辑而产生三个新 Bug。
*   **无法规模化**：对于原型开发，氛围编程很快；但对于严肃的生产级系统，它会导致代码质量参差不齐、架构违规和技术债累累。

**核心转折：**
AI 的瓶颈往往不在于它的编码能力，而在于我们给它的指令充满了歧义。我们需要一种更严谨的方法论——**规范驱动开发 (SDD)**。

---

#### 第二部分：SDD——规格（Spec）即真理（15 分钟）

**2.1 什么是 SDD？**
SDD 颠覆了传统的“代码优先”模式。它主张**规范（Specification）是系统的唯一事实来源**，代码只是规范的实现细节。在这一范式下，工程师的角色从“打字员”转变为“规范架构师”。

**2.2 SDD 的四个关键阶段 (The 4-Phase Pipeline)**：
1.  **定义 (Specify)**：回答“做什么”。专注于用户旅程和验收标准，排除技术栈干扰。
2.  **规划 (Plan)**：回答“怎么做”。确定技术栈、架构模式、数据模型和接口契约。
3.  **分解 (Tasks)**：将复杂的计划拆解为原子化的、可测试的独立任务块。
4.  **执行 (Implement)**：AI 代理根据任务清单逐步生成代码，由人类进行针对性审查而非全量 Review。

**2.3 严谨度光谱（Spectrum of Rigor）**：
您可以根据项目需求选择不同的实施深度：
*   **规范引导 (Spec-First)**：编码前写 Spec 引导初始构建，适合快速原型。
*   **规范锚定 (Spec-Anchored)**：规范与代码同步演进，作为长期资产，是目前微服务治理的主流方案。
*   **规范即源码 (Spec-as-Source)**：最激进模式，人类只修改 Spec，代码由 AI 全量生成，代码成为“易耗品”。

---

#### 第三部分：Harness 工程——为 AI 代理构建“围栏”与“导航”（15 分钟）

**3.1 什么是 Harness 工程？**
如果提示词工程是给马下达指令，上下文工程是给马看地图，那么 **Harness 工程就是马鞍、缰绳和围栏**，确保十匹马能安全地跑在预定航道上。
它是指围绕 AI 代理构建的**完整环境、约束和反馈循环**。

**3.2 为什么我们需要 Harness？**
传统的提示词无法阻止 AI 重写你的整个代码库。Harness 工程通过结构化的外部机制，弥补了模型智能的局限性。

**3.3 软件 Harness 的核心组件**：
*   **验证门禁 (Verification Gates)**：物理性地阻止 AI 违反规则。例如：自动化的 Lint 检查、测试用例和架构一致性测试。
*   **状态管理**：持久化任务进度和会话日志。确保 AI 在长周期、跨会话的任务中不失忆。
*   **反馈循环**：当 AI 出错时（如编译失败），系统应能自动捕捉错误并作为新的上下文反馈给 AI，启动自修复流程。
*   **知识库系统**：所有架构决策和编码原则必须以 `CLAUDE.md` 或 `AGENTS.md` 文件的形式存在于仓库中，作为 AI 的“事实标准”。

**3.4 实证案例：OpenAI 的百万行代码实验**：
2025年底，OpenAI 一个小组通过构建强有力的 Harness 环境，在 5 个月内让 AI 代理生成了约 100 万行生产代码，期间**人类没有手写一行代码**，且开发速度提升了 10 倍。

---

#### 第四部分：工具实战与行动建议（5 分钟）

**4.1 落地工具推荐：GitHub Spec Kit**
这是目前最成熟的 SDD 落地工具包。通过简洁的**斜杠命令**将上述流程集成到开发环境：
*   `/speckit.constitution`：确立项目宪法（技术原则和规范）。
*   `/speckit.specify`：生成功能规范。
*   `/speckit.plan`：生成技术方案。
*   `/speckit.tasks`：生成任务清单。
*   `/speckit.implement`：启动自动化构建。

**4.2 给同事们的经验建议：**
*   **对新手**：不要试图一步到位。先学会使用 `/speckit.clarify` 来补全你的模糊想法。
*   **对进阶者**：探索**对抗性代理模式 (Adversarial Agent Pattern)**，即让一个 AI 负责实现，另一个 AI 专门作为 Verifier 寻找失败点。
*   **组织层面**：将团队的质量标准从“口头约定”转向“机器可读”的 Harness 约束。

**结束语：**
我们正进入一个“意图即源代码”的时代。通过 SDD 和 Harness 工程，我们可以从代码的雕刻师升级为系统灵魂的铸造者。

---

### 宣讲小贴士：
1.  **互动设计**：在讲到“氛围编程”失败案例时，可以邀请现场同事分享一个“AI 改坏了代码”的惨痛经历，以此引入 SDD。
2.  **视觉呈现**：PPT 建议展示 GitHub Spec Kit 生成的 `tasks.md` 的清晰任务列表，直观展示“确定性”。
3.  **针对不同水平**：强调即使不使用复杂工具，在 README 中清晰定义 Specification 也是一种轻量级的 SDD 实践。