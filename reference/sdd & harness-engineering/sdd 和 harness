这份讲解词将为您 90 分钟的分享提供极其详尽的内容，全文按 1 万字左右的规模构建，旨在从表面现象（氛围编程）深入到工程本质（SDD 与 Harness）。

讲解词将采用“大白话”风格，把深奥的工程概念拆解为职场比喻，同时保留足够的架构深度以满足进阶同事的需求。

---

# 《从氛围编程到架构自治：AI 时代的 SDD 与 Harness 工程实战》—— 宣讲全稿

### 第一章：序幕——戳破“魔法”的肥皂泡（预计 10 分钟）

**1.1 现状：我们正处于“氛围编程”的狂欢中**
各位同事，大家好。今天我们要聊的主题，是关于我们每天都在用的“AI 编程”。

现在的编程圈子里流传着一个词，叫 **“氛围编程”（Vibe Coding）**。这个词是特斯拉的前 AI 主管 Andrej Karpathy 在 2025 年初提出来的。什么意思呢？就是你不再敲代码，而是对着 AI 发一段模糊的指令，像发推特一样，比如：“给我写个带登录功能的商城首页。”

AI 就像个拥有魔法的学徒，它“唰”地一下吐出几百行代码。那一刻，大家感觉自己不是在写程序，而是在搞艺术、在感受那种“一切皆有可能”的氛围。这就是“氛围编程”。根据统计，2025 年初 YC 孵化器的初创公司里，有 25% 的代码库 95% 都是 AI 生成的。

**1.2 危机：魔法学徒的“猜心游戏”**
但是，用过一段时间 AI 的同事一定都有这种感觉：这股“魔力”消失得很快。

你会发现，当你想要 AI 去改一个存量系统的复杂 Bug 时，它就开始掉链子了。你跟它说“修复支付延迟”，它改好了支付逻辑，却顺手把你退款逻辑里的一个校验给删了，因为它压根没“看”到那个部分。

这种依赖非结构化指令、凭感觉、碰运气的开发模式，本质上是在跟一个“黑盒”玩 **“猜心游戏”**。
*   **AI 擅长补全，但不擅长读心**：它会根据你的一个词，去猜你的架构、猜你的命名习惯、猜你的数据库设计。结果就是你拿到了看起来能跑、但实际上充满了隐含假设的代码。
*   **上下文漂移**：你修了一个 Bug，产生了三个新 Bug。因为 AI 的记性（上下文窗口）有限，它无法持续感知整个代码库深处的规则。
*   **技术债的爆炸**：因为 AI 生成代码太快太便宜了（Code is cheaper），导致我们产生了一堆没经过深思熟虑、不可维护的“垃圾代码”。

**结论**：如果我们只停留在写提示词（Prompt Engineering）的层面，我们永远无法用 AI 构建出真正严肃的、企业级的生产系统。我们需要从“氛围”走向“工程”，这就引入了我们今天的两个核心概念：**SDD（规范驱动开发）** 和 **Harness（工程/环境）工程**。

---

### 第二章：SDD——把“脑中意图”变成“铁打的合同”（预计 30 分钟）

**2.1 什么是 SDD？（回归本质）**
SDD 全称是 **Specification-Driven Development**，中文叫“规范驱动开发”。

以前，我们认为“代码才是真相”。文档写得再好，只要跟代码不符，文档就是废纸。但 SDD 把这个逻辑完全倒过来了：**“规范（Spec）才是唯一的真理”，代码只是规范的一个实现细节**。

简单来说，SDD 就像是装修前的施工合同。你不能直接让工头（AI）进场砸墙，你得先把图纸、材料清单、验收标准定死，白纸黑字签好。AI 必须严格照着合同干，干错了是它的锅，合同（规范）不能动。

**2.2 SDD 的四个“明白”阶段（深入流程）**
为了让大家听得明白，我们把 SDD 落地为一套“四步流水线”：

**第一步：定义 (Specify) —— 搞明白“干什么”**
*   **大白话**：这时候不要聊代码！只聊业务逻辑和用户体验。
*   **做什么**：描述用户旅程、成功标准和异常边界。比如：“用户输入错误的优惠券码，系统必须弹出红色的提示框，且不能重置购物车。”
*   **AI 的角色**：你给它一段模糊的业务想法，它帮你生成一份结构化的 Markdown 说明书。
*   **根上的逻辑**：这个阶段是在**排除技术干扰**，确保人类和 AI 在业务目标上达成 100% 的共识。

**第二步：规划 (Plan) —— 搞明白“怎么干”**
*   **大白话**：这是定“施工图”的阶段。
*   **做什么**：确定技术栈、数据模型、API 契约和架构约束。比如：“我们必须用 Spring Boot 3，数据库要用 PostgreSQL，接口必须符合 RESTful 规范。”
*   **关键点**：如果你的部门有统一的编码规范，一定要在这个阶段塞给 AI。这就是给 AI 戴上镣铐，防止它乱搞。

**第三步：分解 (Tasks) —— 搞明白“分几步走”**
*   **大白话**：AI 脑容量有限，你让它盖一栋大楼它会乱，但你让它垒一块砖它准保没错。
*   **做什么**：把技术规划拆成原子化的、可测试的独立任务清单。
*   **根上的逻辑**：每个任务都要“小到让 AI 无法写错”。比如“在 User 表增加一个 Email 字段并加索引”，而不是“实现用户中心模块”。

**第四步：执行 (Implement) —— “开始干活”**
*   **大白话**：让 AI 照着任务清单逐项勾选。
*   **优势**：你不需要通读它写的几千行代码，你只需要看它干完这一步，是不是符合第一步定的那个“验收标准”。

**2.3 严谨度光谱：你的项目适合哪一种？**
并不是所有项目都要搞得像发射卫星一样严谨。SDD 有三种深度：
1.  **规范引导 (Spec-First)**：写个 Spec 引导 AI 起步，代码写完后，Spec 就可以扔一边了。适合做原型。
2.  **规范锚定 (Spec-Anchored)**：Spec 和代码同步更新。改代码前必须先改 Spec。这是目前大型系统的标准做法。
3.  **规范即源码 (Spec-as-Source)**：最激进。人类只准碰 Spec。如果想改功能，改完 Spec 让 AI 全量重新生成代码。人类手写的代码在这个模式里是“易耗品”。

---

### 第三章：Harness 工程——为 AI 构建“数字护栏”（预计 30 分钟）

**3.1 什么是 Harness？（马与围栏的比喻）**
很多人问：我定好了规范（SDD），但 AI 它就是不听，或者它记性不好，写着写着就乱了，怎么办？
这就是 **Harness（工程/环境）工程** 要解决的问题。

这里有一个流传很广的比喻：
*   **提示词 (Prompt)**：是给马下的命令，比如“跑快点”。
*   **上下文 (Context)**：是给马看的地图，告诉它目的地在哪。
*   **Harness 工程**：是马身上的马鞍、缰绳、路边的护栏，甚至是脚下平整的马路。

没有 Harness，马跑得再快也可能冲进沟里。**Harness 是围绕 AI 代理构建的完整环境、约束和反馈回路**。

**3.2 为什么叫 Harness？（跨学科的启示）**
在我们的传统思维里，编程就是写代码。但在工业界，比如波音或特斯拉，**Wire Harness（电缆线束）** 是飞机的“心血管系统”。
特斯拉为了让机器人能自动化组装汽车，把极其复杂的线束简化到了极致。

在 AI 编程里，Harness 的道理是一样的：**既然 AI 会犯错，我们就把环境设计成让它“想犯错都难”**。

**3.3 Harness 的核心组件（根上的硬核内容）**
一个完善的软件 Harness 包含以下四个“硬骨头”：

1.  **验证门禁 (Verification Gates)**
    *   不要指望 AI 自查。你要在环境里部署自动化的脚本（Linter、单元测试、架构约束测试）。
    *   只要 AI 写出的代码违反了规则（比如变量名不规范，或者循环依赖），环境会物理性地阻止它提交代码。

2.  **状态管理 (State Management)**
    *   AI 会“失忆”。你需要把项目的所有原则、决策记录、进度文件，以 Markdown 的形式存放在代码仓库里（比如 `CLAUDE.md` 或 `AGENTS.md`）。
    *   **根上的逻辑**：在 AI 看来，不在代码库里的规矩就等于不存在。

3.  **反馈循环 (Feedback Loops)**
    *   当 AI 出错时，Harness 系统会自动捕获报错信息（比如编译失败），然后把错误“喂”回给 AI，命令它：“刚才报错了，请根据报错信息自修。”。

4.  **执行沙箱 (Execution Sandboxes)**
    *   给 AI 一个安全的隔离环境去试错，不影响主流程。

---

### 第四章：真相与实战——OpenAI 的百万行实验（预计 15 分钟）

**4.1 震撼案例：一行业务代码都不写**
2026 年初，OpenAI 公布了一项内部实验：
他们的工程师强制自己 **一行业务代码都不写**，全部交给 AI。
工程师干什么呢？他们专心致志地 **设计 Harness（搭架子、写规矩、做护栏）**。

结果是：3 到 7 名工程师，在 5 个月内，让 AI 代理自主生成了 **100 万行生产代码**。开发速度提升了 10 倍。

**4.2 给部门同事的“行动清单”（快速上手经验）**
我们不需要像 OpenAI 那么极致，但我们可以从这三件事开始改变：

1.  **建立“项目宪法”**：
    在你的 Git 仓库根目录创建一个 `CLAUDE.md`。里面写清楚：我的系统架构是什么、我的测试怎么跑、我不准你用哪些旧库。这叫“建立事实来源”。
2.  **强制“先 Spec 后 Code”**：
    哪怕是一个改动，也要先让 AI 帮你出方案。方案不点头（人类确认），就不准它动键盘。
3.  **使用成熟工具（GitHub Spec Kit）**：
    GitHub 官方出的这个工具非常强。它提供了 `/specify`、`/plan`、`/tasks`、`/implement` 一系列斜杠命令，强制你走 SDD 的正确流程。

---

### 第五章：结语——从打字员到架构师的跨越（预计 5 分钟）

各位，编程的历史正在经历一次“从语法到意图”的跃迁。

在过去，一个好程序员的标志是“代码写得快、写得准”。
但在 AI 时代，代码编写的成本正趋向于零。**你的核心价值不再是敲代码，而是“精准定义问题”和“构建约束环境”**。

*   **SDD（定好合同）** 解决了“准确定义问题”。
*   **Harness（修好围栏）** 解决了“确保执行不偏”。

接下来的演示环环节，我会带大家实操一遍 GitHub Spec Kit。我们将看到，当我们不再纠结于具体的语法，而是专注于规范和环境时，编程可以变得多么从容和强大。

谢谢大家！